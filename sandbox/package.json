{
  "name": "nextgen-eam-sandbox",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host --port 5174",
    "start": "vite --host --port 5174",
    "build": "vite build",
    "preview": "vite preview --host --port 5174"
  },
  "dependencies": {
    "@apollo/sandbox": "^2.7.0",
    "graphql": "^16.9.0"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
