# NextGen EAM Apollo Sandbox

Run the embedded Apollo Sandbox against the local GraphQL API.

## Setup

```bash
yarn install
```

## Run

```bash
yarn dev
# or

yarn start
```

## Configuration

Set a custom GraphQL endpoint with:

```bash
VITE_GRAPHQL_ENDPOINT=http://localhost:4000/graphql
```
