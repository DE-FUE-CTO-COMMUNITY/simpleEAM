query {
  applicationsConnection(
    where: {
      company: { some: { id: { eq: {{companyId}} } } }
      status: { eq: {{status}} }
    }
  ) {
    aggregate {
      count {
        nodes
      }
    }
  }
}