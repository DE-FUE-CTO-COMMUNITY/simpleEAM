query {
  hardwareProducts(
    where: {
      company: { some: { id: { eq: {{companyId}} } } }
      name: { contains: {{searchText}} }
    }
    limit: {{limit}}
    sort: [{ name: ASC }]
  ) {
    id
    name
    lifecycleStatus
    isActive
  }
}