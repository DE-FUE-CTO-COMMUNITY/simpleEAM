query {
  applications(
    where: {
      company: { some: { id: { eq: {{companyId}} } } }
      id: { eq: {{applicationId}} }
    }
    limit: 1
  ) {
    id
    name
    description
    status
    criticality
    technologyStack
    vendor
  }
}