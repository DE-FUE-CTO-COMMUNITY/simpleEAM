query {
  {{entityRoot}}(
    where: {
      company: { some: { id: { eq: {{companyId}} } } }
      {{statusFilterClause}}
      {{relationField}}: { some: { name: { contains: {{relatedNameContains}} } } }
    }
    limit: {{limit}}
  ) {
    id
    name
    {{statusFieldSelection}}
  }
}