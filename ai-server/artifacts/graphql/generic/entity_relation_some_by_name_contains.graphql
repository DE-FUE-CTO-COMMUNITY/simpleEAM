query {
  {{entityRoot}}(
    where: {
      company: { some: { id: { eq: {{companyId}} } } }
      {{statusFilterClause}}
      {{entityWhereClause}}
    }
    limit: {{limit}}
  ) {
    id
    name
    {{statusFieldSelection}}
    {{detailSelectionSet}}
  }
}