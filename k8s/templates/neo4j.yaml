{{- $fullName := include "nextgen-eam.fullname" . -}}
{{- $labels := include "nextgen-eam.labels" . -}}
{{- $neo4j := .Values.neo4j -}}
# ── PersistentVolumeClaim ──────────────────────────────────────────────────────
{{- if $neo4j.persistence.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ $fullName }}-neo4j
  labels: {{- $labels | nindent 4 }}
spec:
  accessModes:
    - {{ $neo4j.persistence.accessMode }}
  resources:
    requests:
      storage: {{ $neo4j.persistence.size }}
  {{- if $neo4j.persistence.storageClass }}
  storageClassName: {{ $neo4j.persistence.storageClass }}
  {{- end }}
---
{{- end }}
# ── Service ───────────────────────────────────────────────────────────────────
apiVersion: v1
kind: Service
metadata:
  name: {{ $fullName }}-neo4j
  labels: {{- $labels | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 7474
      targetPort: 7474
    - name: bolt
      port: 7687
      targetPort: 7687
  selector:
    app.kubernetes.io/name: {{ include "nextgen-eam.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: neo4j
---
# ── Deployment ────────────────────────────────────────────────────────────────
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ $fullName }}-neo4j
  labels: {{- $labels | nindent 4 }}
spec:
  serviceName: {{ $fullName }}-neo4j
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "nextgen-eam.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
      app.kubernetes.io/component: neo4j
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "nextgen-eam.name" . }}
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/component: neo4j
    spec:
      {{- if .Values.global.imagePullSecrets }}
      imagePullSecrets: {{- toYaml .Values.global.imagePullSecrets | nindent 8 }}
      {{- end }}
      containers:
        - name: neo4j
          image: {{ printf "%s:%s" $neo4j.image.repository $neo4j.image.tag }}
          imagePullPolicy: {{ $neo4j.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 7474
            - name: bolt
              containerPort: 7687
          env:
            - name: NEO4J_AUTH
              valueFrom:
                secretKeyRef:
                  name: {{ default (printf "%s-neo4j" $fullName) $neo4j.auth.existingSecret }}
                  key: {{ default "NEO4J_AUTH" (ternary "NEO4J_AUTH" "" (not $neo4j.auth.existingSecret)) }}
            - name: NEO4J_dbms_memory_pagecache_size
              valueFrom:
                configMapKeyRef:
                  name: {{ $fullName }}-config
                  key: NEO4J_PAGECACHE_MEMORY
            - name: NEO4J_dbms_memory_heap_initial__size
              valueFrom:
                configMapKeyRef:
                  name: {{ $fullName }}-config
                  key: NEO4J_HEAP_INITIAL_SIZE
            - name: NEO4J_dbms_memory_heap_max__size
              valueFrom:
                configMapKeyRef:
                  name: {{ $fullName }}-config
                  key: NEO4J_HEAP_MAX_SIZE
          livenessProbe:
            httpGet:
              path: /
              port: 7474
            initialDelaySeconds: 60
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /
              port: 7474
            initialDelaySeconds: 30
            periodSeconds: 10
          resources: {{- toYaml $neo4j.resources | nindent 12 }}
          {{- if $neo4j.persistence.enabled }}
          volumeMounts:
            - name: data
              mountPath: /data
          {{- end }}
      {{- if $neo4j.persistence.enabled }}
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: {{ $fullName }}-neo4j
      {{- end }}
